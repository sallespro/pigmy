# --- secrets: never commit ---
.env
*.env
!*.env.example
har/.env
pop/.env

# --- os / editor ---
.DS_Store

# --- node ---
node_modules/
*.log

# --- pi-gm harness runtime state (derived per workspace) ---
.gm-pi/

# --- lean harness runtime state (derived per workspace) ---
.lean/

# --- agentplug runtime kv ---
.agentplug-kv/

# --- sandbox: agent clones, per-run workspaces, and run logs ---
sand/run/
sand/public/app.js

# --- gm managed runtime (derived / transient) ---
.gm/exec-spool/
.gm/witness/
.gm/gm.db
.gm/gm.db-*
.gm/config-source-cache-default/

# --- separate project with its own git repository ---
pop/

# --- gm session state (managed by the gm skill, not this project) ---
.gm/

# --- skill tooling artifacts, not part of the harness ---
har/.agents/
har/.claude/
har/skills-lock.json
