# Copy to .env and fill in. The key is read once by the server and injected
# into each agent's process environment at spawn -- it is never written into
# a clone or anywhere under run/.
OPENAI_API_KEY=sk-...
